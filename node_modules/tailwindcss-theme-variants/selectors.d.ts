import { Node, Root } from "postcss-selector-parser";
export declare const parse: (selector: string) => Root;
export declare const unparse: (node: Node) => string;
export declare const addParent: (selector: string, parent: string) => string;
